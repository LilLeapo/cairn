# Cairn — 产品 Spec

> Cairn（玛尼堆 / 路标石堆）：行路人把石头一块块垒起来，标记"我到过这里、这条路通"。
> 这个名字本身就是产品的核心动作——**每收拢一层，就是垒一块石头**。

---

## 一句话定义

**显化你和 AI 学习时的探索过程本身，并在该收的时候帮你收。**

市面工具（NotebookLM、Mapify…）显化的是"材料"，让你当消费者；
Cairn 显化的是"你"。

---

## 核心机制——产品的心脏

循环只有一句：

> **AI 看着你已有的图 → 提几个可扩展的方向/细节 → 你决定推不推进 → 不推了，就收一下。**

其中 **"这个开成子图，还是一笔带过？"** 这个动作，**一个顶三个**：

- 判断深度
- 和你对齐
- 制造摩擦

这是整个产品最关键的**交互原语**，也是这一路最被看好的发现。

---

## 底层结构

**递归嵌套的图**：每个点双击进去是一张子图，地图套地图。
**放大＝扩散，缩小＝收拢——同一个动作，两个方向。**

但纯嵌套是**树**。所以必须 **＋横切连接**（跨子图的线）：

> "带宽" 是主干、"KV 缓存分页 ≈ OS 分页"

这是 tree 变 graph 的命门。**没它，就只是个能 zoom 的大纲。**

- **收拢** ＝ 把一层折进它的标题。细节没删、是被**索引**了，随时双击展开；每层有每层的主干。
- 节点是**探索长出来的，不是预生成的**。
- 没钻过的点是**空的**——而"空"本身就是信息：**这个词你提过，但从没真懂。**

---

## 三条铁律（区别于所有现有工具的地方）

1. **收拢必须你来做。**
   AI 只在对的时机逼你用一句话说出主干，**绝不替你印出来**。
   替你印 ＝ NotebookLM 陷阱 ＝ 你没学到。

2. **摩擦永远长成对话的下一句，绝不是测验模块。**
   问连接 / 预测 / 压缩 / 纠错，**不问**定义 / 复述。

3. **深度由"你的目的 ＋ 你已会什么 ＋ 能不能接回上层"定，不由话题定**（话题无底）。
   AI 默认自己猜深浅，**只在承重的岔路口**才把选择权抛给你——问太勤会烦死人。

---

## 架构

**形态是一层长在学习现场的薄层，不是要你专门打开的新 App。**
（不然你懒得把对话搬进去，必死。）

决定**自己接管对话循环**，用 API 写最简陋的聊天界面。

**一张脸，两套循环：**

| 角色 | 职责 |
|---|---|
| **老师** | 回答问题（可替换） |
| **观察者** | 维护图、判断深度、提方向、戳你 |

用户只感觉到一个角色。

> 产品的全部 IP ≈ **一个写得极讲究的观察者 prompt ＋ 一个不断生长的递归图（JSON）**。
> 画布和 UI 全是水管。

---

## 技术决策：纯前端 + 本地优先（local-first）

> **结论翻过来了：v1 不要后端。纯前端（React + idb + 浏览器直连）。**
> 这不只是"能省则省"——对这个产品，它**反而是更对的架构**。

### 存储：IndexedDB 完全够，不需要后端
那张递归图、所有对话、所有子图，全塞进 IndexedDB 毫无问题，用 `idb` 这个 wrapper 即可。
**存储从来就不需要后端。**

### Key：浏览器直连，纯前端也能解决
Anthropic 支持浏览器直连 `api.anthropic.com`，CORS 能过、流式照常：

- 请求 header：`anthropic-dangerous-direct-browser-access: true`
- TS SDK：`dangerouslyAllowBrowser: true`

官方明说的两个合法用法，正好都是本产品的场景：

| 模式 | 做法 | 为什么安全 |
|---|---|---|
| **内部自用** | key 写死在本地配置，跑在自己机器上 | 你自己的机器、你自己的 key，没人偷得走 |
| **BYOK（自带 key）** | 用户粘自己的 key，存进 IndexedDB，前端拿它直连 | **你从头到尾不碰任何人的 key**——连做成静态站发给别人都成立 |

**唯一的反模式 / 唯一会崩的情况：**
把 key 写死进客户端代码、再把页面**公开**出去——那任何访客都能扒走你的 key 替你发请求。
本地自用、或 BYOK，都绕开了它。

### Provider 可插拔
老师本来就是"可替换"的，所以调模型抽成薄薄一层、按 provider 分发。两种接口形态、＋自定义 base URL：

- **Anthropic**：官方 / 兼容 / 代理端点。
- **OpenAI 兼容**：官方 OpenAI、OpenRouter、DeepSeek、本地 Ollama/LM Studio… 凡 OpenAI 协议皆可。

观察者的结构化输出是同一份 JSON schema，喂给两边（Anthropic 的 `input_schema` / OpenAI 的 function `parameters`）。
注意：浏览器直连要端点放行 CORS——第三方代理、本地基本都放行；官方 `api.openai.com` 默认不放行（那就是"上薄代理"的场景）。

### 为什么 local-first 是特性，不是将就
那张图是**你理解的底片**，极私人。让它**永远不离开你的机器**，是产品的特性。
之前担心的那点服务端逻辑（观察者输出的 JSON schema 校验之类）也都是**纯 JS 就能跑**，没有一行非得在服务器上。

### 唯一要等后端的那天
当你想做成"**用户不用自带 key 的托管服务**"——那时再加一层薄代理护住你自己的 key 就行。
**那是 product 阶段的事，不是现在。**

> ⚠️ TODO（动手前核对）：这套 CORS 直连自 2024 年起稳定，但真开写前扫一眼 `docs.claude.com` 确认当前细节。

---

## 版本切分

### v1
- 对话学习
- 一张被解读过、从你探索中长出来的图
- 那个核心循环（提方向 → 你定 → 收拢）
- 图能存下来（治"学完就丢"）

> 图可以丑得要死——因为赌的是**判断准不准**，不是好不好看。

### 往后推
- 四种戳的精细玩法
- 伴侣人格 ＋ 跨会话记忆 ＋ AI 主动起话头
- 漂亮的交互画布
- 指向你"过去其它图"的**跨图连接**（得先攒下一批旧图才有意义）

---

## 整件事压在一个还没验的赌注上

> **能不能让引擎在"提扩展方向"和"感知该收了"这两件事上，判断得准、而且跟你对齐。**

这个还没验完——因为每次都在（很对地）继续打磨结构。

### 真正的下一步

拿一段现成对话，看引擎提的方向、喊停的时机，跟你真实的那几下**"啊哈"**和**"这条够了"**对不对得上。

**验完，才知道这产品立不立得住。**
