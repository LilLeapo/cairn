# Cairn — 产品 Spec

> Cairn（玛尼堆 / 路标石堆）：行路人把石头一块块垒起来，标记"我到过这里、这条路通"。
> 这个名字本身就是产品的核心动作——**每收拢一层，就是垒一块石头**。

---

## 一句话定义

**显化你和 AI 学习时的探索过程本身，并在该收的时候帮你收。**

市面工具（NotebookLM、Mapify…）显化的是"材料"，让你当消费者；
Cairn 显化的是"你"。

---

## 核心机制——产品的心脏

循环只有一句：

> **AI 看着你已有的图 → 提几个可扩展的方向/细节 → 你决定推不推进 → 不推了，就收一下。**

其中 **"这个开成子图，还是一笔带过？"** 这个动作，**一个顶三个**：

- 判断深度
- 和你对齐
- 制造摩擦

这是整个产品最关键的**交互原语**，也是这一路最被看好的发现。

---

## 底层结构

**递归嵌套的图**：每个点双击进去是一张子图，地图套地图。
**放大＝扩散，缩小＝收拢——同一个动作，两个方向。**

但纯嵌套是**树**。所以必须 **＋横切连接**（跨子图的线）：

> "带宽" 是主干、"KV 缓存分页 ≈ OS 分页"

这是 tree 变 graph 的命门。**没它，就只是个能 zoom 的大纲。**

- **收拢** ＝ 把一层折进它的标题。细节没删、是被**索引**了，随时双击展开；每层有每层的主干。
- 节点是**探索长出来的，不是预生成的**。
- 没钻过的点是**空的**——而"空"本身就是信息：**这个词你提过，但从没真懂。**

---

## 三条铁律（区别于所有现有工具的地方）

1. **收拢必须你来做。**
   AI 只在对的时机逼你用一句话说出主干，**绝不替你印出来**。
   替你印 ＝ NotebookLM 陷阱 ＝ 你没学到。

2. **摩擦永远长成对话的下一句，绝不是测验模块。**
   问连接 / 预测 / 压缩 / 纠错，**不问**定义 / 复述。

3. **深度由"你的目的 ＋ 你已会什么 ＋ 能不能接回上层"定，不由话题定**（话题无底）。
   AI 默认自己猜深浅，**只在承重的岔路口**才把选择权抛给你——问太勤会烦死人。

---

## 架构

**形态是一层长在学习现场的薄层，不是要你专门打开的新 App。**
（不然你懒得把对话搬进去，必死。）

决定**自己接管对话循环**，用 API 写最简陋的聊天界面。

**一张脸，两套循环：**

| 角色 | 职责 |
|---|---|
| **老师** | 回答问题（可替换） |
| **观察者** | 维护图、判断深度、提方向、戳你 |

用户只感觉到一个角色。

> 产品的全部 IP ≈ **一个写得极讲究的观察者 prompt ＋ 一个不断生长的递归图（JSON）**。
> 画布和 UI 全是水管。

---

## 版本切分

### v1
- 对话学习
- 一张被解读过、从你探索中长出来的图
- 那个核心循环（提方向 → 你定 → 收拢）
- 图能存下来（治"学完就丢"）

> 图可以丑得要死——因为赌的是**判断准不准**，不是好不好看。

### 往后推
- 四种戳的精细玩法
- 伴侣人格 ＋ 跨会话记忆 ＋ AI 主动起话头
- 漂亮的交互画布
- 指向你"过去其它图"的**跨图连接**（得先攒下一批旧图才有意义）

---

## 整件事压在一个还没验的赌注上

> **能不能让引擎在"提扩展方向"和"感知该收了"这两件事上，判断得准、而且跟你对齐。**

这个还没验完——因为每次都在（很对地）继续打磨结构。

### 真正的下一步

拿一段现成对话，看引擎提的方向、喊停的时机，跟你真实的那几下**"啊哈"**和**"这条够了"**对不对得上。

**验完，才知道这产品立不立得住。**
